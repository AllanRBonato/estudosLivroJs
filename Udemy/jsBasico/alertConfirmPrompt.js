// quando uma função esta dentro de um objeto chama de metodo, quando ta fora chama são funções.

const num1 = Number(prompt('Digite um número: '));
const num2 = Number(prompt('Digite um número: '));

const soma = num1 + num2;

alert(`A soma dos dois número é: ${soma}`);